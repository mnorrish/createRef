{
  "name": "createref",
  "version": "0.0.1",
  "description": "A React.createRef emulation for React < 16.3",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -d --outDir ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "React"
  ],
  "author": "matt@mnorrish.co.nz",
  "license": "MIT",
  "devDependencies": {
    "@types/react": "^16.3.5",
    "typescript": "^2.8.1"
  }
}
